import JSConfetti from "js-confetti";
const jsConfetti = new JSConfetti();

export const config = {
  emojis: ["🦄", "🌈", "⚡️", "💥", "✨", "💫", "🌸"],
  emojiSize: 50,
  confettiNumber: 15,
};

const yeehaw = jsConfetti;

export default yeehaw;
